version: '3.9'

services:
  vacation-dashboard:
    build: .
    container_name: vacation-dashboard
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
